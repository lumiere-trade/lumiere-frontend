/**
 * Prophet Mutation Hooks
 * React Query mutations for Prophet AI chat
 *
 * Note: Prophet uses SSE streaming (sendChatMessageStream)
 * handled directly in components, not via React Query mutations
 */

// Currently no mutations needed - Prophet uses SSE streaming
// All chat interactions handled via sendChatMessageStream in components
export {}
